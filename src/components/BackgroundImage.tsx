interface BackgroundImageProps {
  activePhase?: number;
}

// Background image removed per user request. Kept as a no-op component
// to preserve existing imports across the app.
const BackgroundImage = (_props: BackgroundImageProps) => null;

export default BackgroundImage;
