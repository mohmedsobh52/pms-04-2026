

## التنفيذ المباشر لشاشة النتائج المفصلة

سأقوم الآن بتنفيذ:

### 1. إنشاء جدول `offer_requests` في قاعدة البيانات
- حفظ نص الطلب والنتائج
- ربط بالمستخدم
- تفعيل RLS

### 2. تحديث `RequestOfferDialog.tsx`
- إضافة شاشة النتائج (Results View)
- جدول مفصل للمواد والأسعار والموردين
- بطاقة ملخص + توصيات + ملاحظات السوق
- حفظ تلقائي في قاعدة البيانات

### الملفات المتأثرة
| الملف | التغيير |
|-------|---------|
| Migration | إنشاء جدول `offer_requests` |
| `RequestOfferDialog.tsx` | إضافة Results View + جدول + حفظ DB |

